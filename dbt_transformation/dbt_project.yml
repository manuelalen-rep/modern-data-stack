name: 'modern_data_pipeline'
version: '1.0.0'
config-version: 2


profile: 'modern_data_pipeline'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
models:
  modern_data_pipeline:
    # Cambiamos staging por silver para que coincida con tu carpeta
    silver:
      +materialized: view
      +schema: silver
    # Cambiamos marts por gold
    gold:
      +materialized: table
      +schema: gold